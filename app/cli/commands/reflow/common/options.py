# app/cli/commands/reflow/common/options.py

"""
Shared CLI options for Reflow commands.

Currently empty.

Global execution options such as:

    --dry-run
    --debug
    --log-level

are provided by:

    app.cli.commands.main

and should not be duplicated inside
individual Reflow commands.

This module exists for architectural consistency
and future shared command options.
"""
