Make sure everything is working fine, no errors

Later
generate GitHub workflow
generate venv based python version
generate necessary files like .pyproject.toml
install needed dev dependencies based on .pyproject.toml
maybe using --init
options to have release-dev.yml workflow
some configurations workflow changes
we can more flexible using branch
use profile like custy.toml for any configurations to minimize parameter, see project_generator repo project
good documentation for users and developers

use docker
use GitHub packages
better todo
GitHub release
setup GitHub project

Make sure everything is working fine, no errors

=====================================================

try use GitLab workflow
try change tag version
try make docker image, package, release with script on GitHub
try make docker image, package, release with script on gitlab

=============

path title for every file
make sure __init__.py on every folder inside site-packages if python project detected.
